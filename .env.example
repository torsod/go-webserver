# Server port
PORT=3000

# PostgreSQL connection URL
DATABASE_URL=postgres://localhost:5432/go_webserver?sslmode=disable

# JWT secret for auth tokens (change in production)
JWT_SECRET=dev-secret-change-in-production

# Optional: Public API on a separate port
# PUBLIC_API_PORT=3001
